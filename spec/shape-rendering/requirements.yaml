schema_version: 1
kind: spec-requirements
status: draft

work_item:
  id: shape-rendering
  title: Shape rendering property
  domain: SVG 2 Painting
  profile: null
  summary: Preserve parser-visible `shape-rendering` hint state for SVG shapes so later renderers can choose speed, crispness, or geometric precision tradeoffs.
  non_goals:
    - Performing anti-aliasing or device-pixel alignment.
    - Implementing renderer-specific shape-quality tradeoffs.
    - Applying the hint to non-shape rendering surfaces.

source_context:
  spec_map: null
  map_items: []
  canonical_sources:
    - id: svg2-shape-rendering-property
      title: "SVG 2 Painting: The shape-rendering property"
      url: https://www.w3.org/TR/SVG/painting.html#ShapeRenderingProperty
      version: SVG 2 Editor's Draft
      publisher: W3C
      accessed: 2026-07-26
      normativity: normative
    - id: svg2-styling-presentation-attributes
      title: "SVG 2 Styling: Presentation attributes"
      url: https://www.w3.org/TR/SVG/styling.html#PresentationAttributes
      version: SVG 2 Editor's Draft
      publisher: W3C
      accessed: 2026-07-26
      normativity: normative
  source_notes:
    - SVG 2 defines `shape-rendering` as an inherited visual property with values `auto`, `optimizeSpeed`, `crispEdges`, and `geometricPrecision`.
    - The initial value is `auto`, the computed value is as specified, and the property applies to shapes.
    - The property is a rendering hint about tradeoffs among speed, crisp edges, and geometric precision.

scope:
  includes:
    - Parser output for the supported `shape-rendering` property on SVG shape elements.
    - Initial value `auto`.
    - Inheritance from parent elements.
    - Supported cascade inputs: presentation attributes, class style rules, and inline style declarations.
    - Valid keywords `auto`, `optimizeSpeed`, `crispEdges`, and `geometricPrecision`.
    - Malformed or unsupported values leaving the current inherited or default value unchanged.
  excludes:
    - Renderer behavior for anti-aliasing, pixel snapping, or geometric precision.
    - Full CSS selector support beyond the package's current supported subset.
  assumptions:
    - The package can expose the parsed value as an enum-like property on existing parser-facing paint/style attributes.
    - CSS keyword parsing may match the supported keywords case-insensitively while preserving enum meaning rather than raw spelling.

terminology:
  - id: term-shape-rendering
    term: shape-rendering
    definition: An inherited SVG rendering hint that tells the user agent how to balance speed, crisp edges, and geometric precision for vector shapes.
    source_refs:
      - svg2-shape-rendering-property
  - id: term-crisp-edges
    term: crispEdges
    definition: A rendering hint indicating that clean edge contrast should be emphasized.
    source_refs:
      - svg2-shape-rendering-property
  - id: term-geometric-precision
    term: geometricPrecision
    definition: A rendering hint indicating that geometric precision should be emphasized.
    source_refs:
      - svg2-shape-rendering-property

structures:
  - id: S001
    name: Shape rendering parser state
    kind: structure
    source_refs:
      - svg2-shape-rendering-property
    fields:
      - name: shapeRendering
        type: keyword enum
        cardinality: one
        position: null
        constraints:
          - Valid values are `auto`, `optimizeSpeed`, `crispEdges`, and `geometricPrecision`.
          - Initial value is `auto`.
          - The property is inherited.
          - Invalid values leave the current inherited or default value unchanged.
        requirement_refs:
          - R001
          - R002
          - R003
          - R004
        source_refs:
          - svg2-shape-rendering-property

requirements:
  - id: R001
    normative_strength: MUST
    kind: structure
    statement: "`shape-rendering` values must be limited to `auto`, `optimizeSpeed`, `crispEdges`, and `geometricPrecision`."
    applies_to:
      - SVG shape elements in parser output
    source_refs:
      - svg2-shape-rendering-property
    testability: testable
    notes: []
  - id: R002
    normative_strength: MUST
    kind: behavior
    statement: "`shape-rendering` must default to `auto` when no cascaded value is supplied."
    applies_to:
      - SVG shape elements in parser output
    source_refs:
      - svg2-shape-rendering-property
    testability: testable
    notes: []
  - id: R003
    normative_strength: MUST
    kind: behavior
    statement: "`shape-rendering` must inherit from parent elements."
    applies_to:
      - SVG shape elements in parser output
    source_refs:
      - svg2-shape-rendering-property
    testability: testable
    notes: []
  - id: R004
    normative_strength: MUST
    kind: behavior
    statement: The parser-visible value must preserve whether the author selected `auto`, `optimizeSpeed`, `crispEdges`, or `geometricPrecision`.
    applies_to:
      - SVG shape elements in parser output
    source_refs:
      - svg2-shape-rendering-property
    testability: testable
    notes: []
  - id: R005
    normative_strength: MUST
    kind: behavior
    statement: A supported `shape-rendering` presentation attribute must map into the same parser-visible property state as a corresponding style declaration.
    applies_to:
      - SVG presentation attributes
    source_refs:
      - svg2-styling-presentation-attributes
      - svg2-shape-rendering-property
    testability: testable
    notes: []

algorithms: []

invalid_or_unsupported_inputs:
  - id: I001
    condition: A `shape-rendering` value is malformed or outside `auto`, `optimizeSpeed`, `crispEdges`, and `geometricPrecision`.
    source_behavior: UNSPECIFIED
    required_behavior: Invalid CSS property values do not produce a valid specified value; this parser work item must leave the current inherited or default value unchanged.
    decision_required: false
    source_refs:
      - svg2-shape-rendering-property

recommendations: []

derived_requirements: []

implementation_choices:
  - id: IC001
    topic: Parser-only storage
    statement: This work item may only preserve parsed property state; actual renderer tradeoffs can be implemented by later rendering back-end work.
    allowed_by:
      - Current package scope
      - User-directed bullet-by-bullet implementation workflow
    source_refs:
      - svg2-shape-rendering-property

ambiguities: []

dependencies:
  - id: DEP001
    kind: source-section
    title: SVG 2 Styling presentation attributes
    reason: "`shape-rendering` can be supplied as a presentation attribute as well as through CSS declarations."
    source_refs:
      - svg2-styling-presentation-attributes

profile_data: {}

traceability:
  - item_id: R001
    item_type: requirement
    source_refs:
      - svg2-shape-rendering-property
    map_item_refs: []
  - item_id: R002
    item_type: requirement
    source_refs:
      - svg2-shape-rendering-property
    map_item_refs: []
  - item_id: R003
    item_type: requirement
    source_refs:
      - svg2-shape-rendering-property
    map_item_refs: []
  - item_id: R004
    item_type: requirement
    source_refs:
      - svg2-shape-rendering-property
    map_item_refs: []
  - item_id: R005
    item_type: requirement
    source_refs:
      - svg2-styling-presentation-attributes
      - svg2-shape-rendering-property
    map_item_refs: []
  - item_id: I001
    item_type: invalid_input
    source_refs:
      - svg2-shape-rendering-property
    map_item_refs: []
